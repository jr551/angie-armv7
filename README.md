# angie-armv7

Builds the Angie web server (nginx fork) from source for `linux/arm/v7`, since upstream
publishes only `amd64` and `arm64`. Image is pushed to `ghcr.io/jr551/angie:armv7`.

Used on a MikroTik L009UiGS-2HaxD container.
